/* style.css */
/* Base utilitaria + componentes pequeños para acompañar Tailwind. */

:root {
  --ring: 0 0 0 3px rgba(124, 58, 237, 0.4);
}

html:focus-within {
  scroll-behavior: smooth;
}

/* Botones */
.btn-primary {
  @apply inline-flex items-center justify-center px-5 py-3 rounded-2xl bg-primary text-white font-semibold shadow-smooth transition transform-gpu hover:-translate-y-0.5 hover:shadow-lg focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/60 active:translate-y-0;
}
.btn-secondary {
  @apply inline-flex items-center justify-center px-5 py-3 rounded-2xl bg-white/10 text-white font-semibold border border-white/10 backdrop-blur transition hover:bg-white/15 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/60;
}
.btn-ghost {
  @apply inline-flex items-center justify-center px-4 py-2 rounded-xl border border-white/10 text-zinc-200 hover:text-white hover:bg-white/5 transition focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/60;
}

/* Secciones */
.section {
  @apply py-20 md:py-28;
}
.container {
  @apply max-w-6xl mx-auto px-4 md:px-6;
}
.section-title {
  @apply text-3xl md:text-4xl font-extrabold tracking-tight;
}
.section-text {
  @apply mt-4 text-zinc-300 max-w-2xl leading-relaxed;
}

/* NAV */
.nav-link {
  @apply px-3 py-2 rounded-lg text-zinc-300 hover:text-white hover:bg-white/5 transition focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/60;
}
.nav-link.is-active {
  @apply text-white bg-white/10;
}

/* Cards */
.card {
  @apply rounded-3xl overflow-hidden bg-white/5 border border-white/10 hover:border-white/20 transition-shadow shadow-smooth;
}
.card-media {
  @apply relative aspect-[3/2] overflow-hidden;
}
.card-img {
  @apply w-full h-full object-cover transition-transform duration-700 ease-out;
}
.card-overlay {
  @apply absolute inset-0 bg-gradient-to-t from-ink/80 via-ink/10 to-transparent opacity-0 transition-opacity duration-500;
}
.card-badge {
  @apply absolute top-3 left-3 px-3 py-1 text-xs font-semibold rounded-full bg-ink/80 border border-white/10;
}
.card-body {
  @apply p-5;
}
.card-title {
  @apply text-lg font-bold;
}
.card-desc {
  @apply mt-1 text-sm text-zinc-300;
}
.card:hover .card-img {
  transform: scale(1.06);
}
.card:hover .card-overlay {
  @apply opacity-100;
}

/* Stats */
.stat {
  @apply p-4 rounded-2xl bg-white/5 border border-white/10 backdrop-blur;
}
.stat-number {
  @apply text-2xl font-extrabold;
}
.stat-label {
  @apply text-xs uppercase tracking-wide text-zinc-400;
}
.check {
  @apply inline-flex items-center justify-center size-5 rounded-full bg-white/10 text-zinc-100 leading-none;
}

/* Formularios */
.form-label {
  @apply block text-sm font-medium text-zinc-300 mb-1;
}
.form-input {
  @apply w-full px-4 py-3 rounded-2xl bg-white/5 border border-white/10 text-white placeholder:text-zinc-500 outline-none focus:ring-2 focus:ring-primary/60;
}
.form-error {
  @apply mt-1 text-xs text-rose-300;
}

/* Accesibilidad: reducir movimiento */
@media (prefers-reduced-motion: reduce) {
  * {
    animation: none !important;
    transition: none !important;
    scroll-behavior: auto !important;
  }
}

/* Hero Canvas fallback */
#hero-canvas {
  background: radial-gradient(1000px 500px at 20% 30%, rgba(124,58,237,0.18), transparent),
              radial-gradient(800px 400px at 80% 70%, rgba(56,189,248,0.15), transparent),
              linear-gradient(180deg, #0b1220, #0f172a);
}
